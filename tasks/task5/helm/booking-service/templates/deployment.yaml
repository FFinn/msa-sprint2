apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-service-{{ .Values.version }}
  labels:
    app: booking-service
    version: {{ .Values.version }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: booking-service
      version: {{ .Values.version }}
  template:
    metadata:
      labels:
        app: booking-service
        version: {{ .Values.version }}
    spec:
      containers:
        - name: booking-service
          image: "{{ .Values.image.name }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: ENABLE_FEATURE_X
              value: {{ .Values.ENABLE_FEATURE_X | quote }}
            - name: SERVICE_VERSION
              value: {{ .Values.SERVICE_VERSION | quote }}
            {{- range .Values.env }}
            - name: {{ .name }}
              value: {{ .value | quote }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /ping
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ping
              port: 8080
            initialDelaySeconds: 2
            periodSeconds: 5
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
